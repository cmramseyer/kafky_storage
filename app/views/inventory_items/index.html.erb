<% content_for :title, "Inventory" %>

<%= turbo_stream_from "inventory_items" %>

<section class="page-header">
  <div>
    <p class="eyebrow">Stock operations</p>
    <h1>Inventory</h1>
  </div>
</section>

<div id="inventory_items">
  <%= render "inventory_items/inventory_list", inventory_items: @inventory_items %>
</div>
