<span id="<%= dom_id(inventory_item, :available_quantity) %>" class="inventory-quantity-update inventory-quantity-update--<%= direction %>">
  <%= render "inventory_items/available_quantity", inventory_item: inventory_item %>
</span>
